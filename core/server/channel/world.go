package channel

/*// MSWorld represents a world in a channel
type MSWorld struct {
	channelID int
	players   map[int64]*world.MSPlayer
}

func NewMSWorld(channelID int) *world.MSWorld {
	return &MSWorld{
		channelID: channelID,
		players:   make(map[int64]*world.MSPlayer),
	}
}

func (w *MSWorld) GetPlayer(id int64) *world.MSPlayer {
	return w.players[id]
}

func (w *MSWorld) AddPlayer(p *world.MSPlayer) {
	w.players[p.charModel.ID] = p
}
*/
